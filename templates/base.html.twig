<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Symfony To-Do List{% endblock %}</title>

    <!-- ✅ Ajout de Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-light">

    <!-- ✅ Barre de navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('task_index') }}">📝 To-Do List</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('task_index') }}">📋 Toutes les tâches</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('task_index', {filter: 'pending'}) }}">📌 À faire</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-success" href="{{ path('task_new') }}">➕ Ajouter une tâche</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- ✅ Contenu principal -->
    <div class="container mt-4">
        {% block body %}{% endblock %}
    </div>

    <!-- ✅ Ajout du script Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    {% block javascripts %}{% endblock %}
</body>
</html>
